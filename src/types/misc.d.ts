import { DateValue } from "../../node_modules_bak/@mantine/dates/lib";

export interface IContactForm {
    name: string;
    email: string;
    message: string;
    phone: string;
    dateTime: DateValue;
};